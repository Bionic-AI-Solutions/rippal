apiVersion: v1
kind: Service
metadata:
  name: rippal-ssh-service
  namespace: rippal
  labels:
    app: rippal
spec:
  type: ClusterIP
  ports:
  - port: 22
    targetPort: 22
    protocol: TCP
    name: ssh
  selector:
    app: rippal
