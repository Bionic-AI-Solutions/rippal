apiVersion: v1
kind: Service
metadata:
  name: rippal-ssh-service
  namespace: rippal
  labels:
    app: rippal
spec:
  type: ClusterIP
  ports:
  - port: 2223
    targetPort: 2223
    protocol: TCP
    name: ssh
  selector:
    app: rippal
