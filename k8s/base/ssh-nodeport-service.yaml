apiVersion: v1
kind: Service
metadata:
  name: rippal-ssh-nodeport
  namespace: rippal
  labels:
    app: rippal
spec:
  type: NodePort
  ports:
  - port: 2223
    targetPort: 2223
    nodePort: 30224
    protocol: TCP
    name: ssh
  selector:
    app: rippal
