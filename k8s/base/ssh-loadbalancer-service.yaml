apiVersion: v1
kind: Service
metadata:
  name: rippal-ssh-loadbalancer
  namespace: rippal
  labels:
    app: rippal
spec:
  type: LoadBalancer
  ports:
  - port: 22
    targetPort: 22
    protocol: TCP
    name: ssh
  selector:
    app: rippal
